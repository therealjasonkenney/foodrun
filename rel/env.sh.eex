#!/bin/sh

# Note, this only works when using the docker image and debian linux.
# But, it keeps things simple in assuming a production target.
NODE_IP=`hostname -i`

# # Set the release to work across nodes.
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=<%= @release.name %>@${NODE_IP}
